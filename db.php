<?php

    // Database Connection Establiment
    // $server = "localhost";
    // $username = "root";
    // $password = "";
    // $con = mysqli_connect($server, $username, $password);
    // if(!$con){
    //     die("connection failed". mysqli_error());
    // }
    // else{
    //     echo "Successfully connected";
    // }

    // Select A Database
    // mysqli_select_db($con, 'trip');

    // CRUD Functions
    // $update = mysqli_query($con, "UPDATE `trip` SET `name` = 'aryan' WHERE `trip`.`sno` = 3;");
    // $delete = mysqli_query($con, "DELETE FROM `trip` WHERE `trip`.`sno` = 3;");
    // $insert = mysqli_query($con, "INSERT INTO `trip`.`trip` (`name`, `age`, `gender`, `email`, `phone`, `other`, `dd`) VALUES ('$name', '$age', '$gender', '$email', '$phone', '$desc', current_timestamp());");
    // $retrieve = mysqli_query($con, "SELECT * FROM trip");
    
    // Iterate Over The Retrieved Data
    // if (mysqli_num_rows($retrieve) > 0) {
    //     $i=0;
    //     while($row = mysqli_fetch_array($retrieve)) {
    //         if($row['name'] == 'aryan'){
    //             mysqli_query($con, "DELETE FROM `trip` WHERE `trip`.`name` = 'aryan';");
    //         }
    //         echo '<Br>';
    //         echo $row['name'];
    //         $i++;
    //     }
    // }

    // Close Connection
    // mysqli_close($con);

?>
